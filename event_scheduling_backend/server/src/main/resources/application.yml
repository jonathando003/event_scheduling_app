spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jonathando
    username: postgres
    password: postgres
    spring.datasource.driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        default_schema: public
